# WebHookURL等、送信媒体の確認 → 反映済み
確認できるように「てすと」などのお試し送信機能があると面白い

# ログについて → 反映済み
膨大になるのが想定できるため、何時から何時までのような制限を掛けれるといいかも

# 決算情報 → 反映済み
これも何日のようなフィルタを掛けたい。
tickerフィルタ、銘柄名フィルタ、日付フィルタなど、ユーザ体験を向上させるようなものが欲しい。

他にも、ユーザ体験を向上させることができるような機能があったら、ブレスト段階から考え、ばしばし付け足していきたい。(趣味の開発なので凝れるだけ凝ります)

# Discord/Slackにメンション機能 → 反映済み 
メンションするか否かという送信媒体ごとの設定を追加したかったのを忘れていた。

- メンションするか
- メンションするならなんというユーザ名(ユーザID)か(例えばeveryoneでも良い)

# スマホ用UI → 反映済み
用意しないこととする。
スマホで開かれた場合は`スマホに対応していません。PCで開いてください。`のような画面を表示することにする。
将来拡張としてスマホに対応させたCSSを用意する。